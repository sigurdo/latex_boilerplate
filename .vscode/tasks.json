{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build report container",
            "type": "shell",
            "command": "docker-compose build"
        },
        {
            "label": "Compile report",
            "type": "shell",
            "command": "docker-compose up"
        }
    ]
}
